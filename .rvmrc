
if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p136@mongorails" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p136@mongorails"
else
  rvm --create use  "ruby-1.9.2-p136@mongorails"
fi
